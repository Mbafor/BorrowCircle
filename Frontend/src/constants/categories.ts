// Mirrors Backend/src/constants/categories.ts — the frontend and backend
// workspaces are independent (no cross-imports), so this fixed list is
// duplicated here. Keep both in sync with docs/BorrowCircle_MVP_v2.md.
export const CATEGORIES = ['Electronics', 'Academic', 'Fashion', 'Sports', 'Creative Tools'] as const;
