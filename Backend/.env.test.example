PORT=4001
NODE_ENV=test
DATABASE_URL=postgresql://borrowcircle:borrowcircle@localhost:5433/borrowcircle_test

ALLOWED_EMAIL_DOMAINS=st.knust.edu.gh

FRONTEND_URL=http://localhost:5173

JWT_ACCESS_SECRET=test-only-secret-do-not-use-in-prod
JWT_ACCESS_EXPIRES_IN=15m

REFRESH_TOKEN_EXPIRES_IN_DAYS=7

PASSWORD_RESET_EXPIRES_IN_MINUTES=60

# Low cost factor keeps the test suite fast; never use this low a value outside tests.
BCRYPT_SALT_ROUNDS=4
